name: exhaust_app
description: OBD2 Engine Sound Simulator
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^5.2.1
  flutter_bluetooth_serial: ^0.4.0
  permission_handler: ^11.0.0

flutter:
  uses-material-design: true
